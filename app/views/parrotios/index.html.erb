

    <section id="portfolio" class="bg-light-gray">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <h1 class="section-heading">ios</h1>
                </div>
            </div>
            <% @parrotios.each do |ios| %>
            
                <div class="col-md-6">

                        <div class="img-responsive" alt="">
                            
                        </div>
                        <a href="<%= ios.linkpath %>"><%= image_tag(ios.binary_data, size: "550x200") %></a>
                        <h4><%= ios.description %></h4>
                </div>
                
            <% end %>
        </div>
    </section>
    
